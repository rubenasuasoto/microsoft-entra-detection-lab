# Security Policy

This repository is a defensive lab with synthetic data only.

Do not submit real tenant logs, credentials, tokens, app secrets, tenant identifiers, private hostnames or production data in issues, pull requests or fixtures.

The demo is static and must remain free of forms, uploads, backend calls and live Microsoft Graph calls.
